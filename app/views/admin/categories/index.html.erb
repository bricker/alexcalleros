<%= link_to "Add a Category", new_admin_category_path %>

<ul>
<% @categories.each do |category| %>
  <li><%= link_to category.title, edit_admin_category_path(category) %></li>
<% end %>
</ul>

<% if @categories.empty? %>
  There are no categories.
<% end %>
